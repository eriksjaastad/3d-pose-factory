# Python
__pycache__/
*.pyc

# Virtualenvs
.venv/
venv/
scripts/.venv/
scripts/venv/

# Data
data/
*.log

# Secrets
.r2_credentials
.env
shared/.env
.pod_id
*.key
*.pem
runpod
runpod.pub

# Command queue (contains command history)
ops_queue/

# OS/editor
.DS_Store
.vscode/
character-creation/scripts/.pod_id
pose-rendering/scripts/.pod_id
repomix-output.xml
.env
.env.doppler-backup

# Blender/Mixamo rig downloads — re-fetched from R2 by shared/scripts/pod_agent.sh
# when the directory is empty. Never commit them (they were tracked until 2026-08-12).
pose-rendering/downloads/
**/downloads/*.fbx
